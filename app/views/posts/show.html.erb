<div id="post_content">
  <h1 class="title">
    <%= @post.title %>
  </h1>
  <p class="date" title= "<%= @post.created_at.strftime("%B, %d, %Y, %z") %>">
    Submitted <%= time_ago_in_words(@post.created_at) %> Ago 
  </p>

  <p class="body">
    <%= @post.body %>
  </p>
</div>